# GCP Gemini Flash client wrapper
# - Order parsing (NL → structured spec)
# - Sharpness/blur detection (image → score 0-1)
# - Aesthetic + semantic + caption scoring (image + description → JSON)
# - Motion intensity scoring (image sequence → score 0-1)
# Use: google.cloud.aiplatform / vertexai.generative_models
